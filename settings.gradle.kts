pluginManagement {
    repositories {
        google()
        mavenCentral()
        // Optional mirror: uncomment if Google/MavenCentral are blocked.
        // maven { url = uri("https://maven-mirror.example.com") }
    }
}

rootProject.name = "Trippify"
include(":app")
